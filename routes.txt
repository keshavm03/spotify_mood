working url


http://127.0.0.1:8000/tracks/recent-tracks?access_token=ACESS_TOKEN 




working routes

under tracks.py
@router.get("/refresh_access_token") //i think we are using this as 
it is called via tracks, 
@router.get("/recent-tracks")

under auth.py
@router.get("/callback")
@router.get("/login")
@router.get("/refresh-token")



methods
in app.spotify_api
we have two methods 
get_recentry_played
refresh_access_token


Endpoints

inside main.py
app.include_router(auth_router, prefix="/auth")
app.include_router(tracks_router, prefix="/tracks")